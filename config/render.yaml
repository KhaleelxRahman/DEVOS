services:
  - type: web
    name: devos-backend
    runtime: python
    rootDir: 03-backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
